# PII Processor — Python dependencies
# Install with:  pip install -r requirements.txt
# Then download NLP models:
#   python -m spacy download en_core_web_sm
#   python -m spacy download xx_ent_wiki_sm

# ── Core NLP / PII detection ──────────────────────────────────────────────────
presidio-analyzer==2.2.354
presidio-anonymizer==2.2.354
spacy>=3.4,<4.0

# ── Document reading ──────────────────────────────────────────────────────────
python-docx>=1.1.0
pdfminer.six>=20221105

# ── CLI helper (used by spacy download) ───────────────────────────────────────
click>=8.0

# ── Standard library — no install needed (listed for documentation only) ──────
# json, logging, os, re, sys, threading, tkinter, typing,
# urllib, tempfile, shutil, subprocess, datetime
